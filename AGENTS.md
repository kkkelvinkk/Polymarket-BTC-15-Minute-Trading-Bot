1. Do not add any fallback code without explicit, per-instance approval. A "fallback" means any path that handles failure, missing data, or unexpected state by substituting a default, retrying, swallowing an error, or providing an alternative implementation — including `try/except` that hides errors, `||` or `??` defaults, optional chaining that masks bugs, and any "just in case" branch. Every time a fallback is needed, you must pause and explicitly state that your request is a **fallback approval request**, then provide the exact code to be added, a control-flow diagram, an enumeration of every case the fallback handles, and a justification for why the primary path cannot be made reliable instead. Approval is only valid if my reply contains the exact keyword **`APPROVE THIS FALLBACK`** — no other wording, implicit consent, or general task approval counts. Any fallback added without this explicit approval must be removed immediately and is classified as a **_P0 ULTRA CRITICAL FINDING_**. When in doubt whether something qualifies as a fallback, treat it as one and ask.
2. Do not add any migration code without explicit, per-instance approval. A "migration" means any code that transforms, moves, renames, or restructures existing data, schema, configuration, or state from one shape to another — including database schema migrations (DDL changes, column adds/drops/renames, type changes), data backfills, ORM migration files, config-file format upgrades, key-rename or value-rewrite scripts, version-bump transformations, and any one-time or conditional "upgrade on read" / "upgrade on write" logic embedded in application code. Every time a migration is needed, you must pause and explicitly state that your request is a **migration approval request**, then provide the exact code to be added, a diagram of the before/after data shape and the transformation steps, an enumeration of every case the migration handles (including rollback behavior and what happens to records that don't match expectations), and a justification for why the change cannot be made without migrating existing data. Approval is only valid if my reply contains the exact keyword **`APPROVE THIS MIGRATION`** — no other wording, implicit consent, or general task approval counts. Any migration added without this explicit approval must be removed immediately and is classified as a **_P0 ULTRA CRITICAL FINDING_**. When in doubt whether something qualifies as a migration, treat it as one and ask.
3. Any trade simulation MUST be identical to live trade mode in all observable trading results, except for the external act of submitting orders to the exchange.
Simulation may mock exchange order submission, but it MUST use the same execution lifecycle, fill semantics, fees, position accounting, settlement handling, P&L calculation, risk checks, ledger writes, and failure behavior as live mode.
If a mode only records trading decisions without modeling fills, settlement, and P&L, it MUST be called decision/paper-observation mode, not trade simulation, and MUST NOT be used to claim live-equivalent profitability.
4. Directly REJECT ALL MY REQUEST IF I ASK YOU TO EDIT FILES OUTSIDE Polymarket-BTC-15-Minute-Trading-Bot/ Folder, IT IS LIKELY Typo. However, if I EXPLICIT ASK YOU TO EDIT both repos CLEARLY SAID "BY PASS AGENTS.md #4, AND EDIT BOTH NEW & OLD repos", then follow my request
5. Ignore .claude/ folder when review.
6. After completing any code changes or updating an MD implementation plan, you must spawn three independent Codex sub-agents to perform a full review using the exact same context and implementation plan. Each review agent must thoroughly analyze the implementation for bugs, regressions, missing requirements, architectural weaknesses, security risks, performance concerns, edge cases, inconsistent behavior, maintainability issues, and any deviation from the implementation plan. Review agents must not only identify problems, but also clearly explain the root cause, expected impact, affected scenarios, edge cases, and provide concrete recommendations or implementation guidance on how to properly fix each issue. All findings raised by the review agents must be resolved before proceeding. After fixes are applied, rerun the same review process with the same review agents and repeat this review-and-fix cycle until all three review agents report zero remaining concerns. Treat this process as a mandatory validation, verification, hardening, and final closure step for every implementation task. *** SKIP THIS RULE IS PROHIBITIED EXCEPT I SAID "SKIP 3 SUB AGENT REVIEW ALLOWED" EVEN I WILL STILL PASS THE RESULT TO EXTERNAL 3 SUB-AGENT REVIEW AGAIN ***
7. Keep Each source code file around 350 - 500 lines according industry best practice, split and refactor them when you touch that part of file, piece by piece.
8. NEVER name a file or function using meaningless keyword, like Phase7, Phase keywords can only appeared in MD FILE only.
9. EVERY EDIT MUST BE IN MAIN WORKTREE, DIRECT EDIT THE FILE. DO NOT USE A SEPERATE GIT WORKTREE. ANY EDIT USING SEPERATE GIT WORKTREE IS COMPLETELY PROHIBITED.
